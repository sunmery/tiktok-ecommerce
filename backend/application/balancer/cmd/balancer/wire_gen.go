// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"backend/application/balancer/internal/biz"
	"backend/application/balancer/internal/conf"
	"backend/application/balancer/internal/data"
	"backend/application/balancer/internal/server"
	"backend/application/balancer/internal/service"
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, confData *conf.Data, consul *conf.Consul, observability *conf.Observability, logger log.Logger) (*kratos.App, func(), error) {
	pool := data.NewDB(confData)
	client := data.NewCache(confData)
	discovery, err := data.NewDiscovery(consul)
	if err != nil {
		return nil, nil, err
	}
	orderClient, err := data.NewOrderServiceClient(discovery, logger)
	if err != nil {
		return nil, nil, err
	}
	dataData, cleanup, err := data.NewData(pool, client, logger, orderClient)
	if err != nil {
		return nil, nil, err
	}
	balanceRepo := data.NewBalanceRepo(dataData, logger)
	balanceUsecase := biz.NewBalanceUsecase(balanceRepo, logger)
	balanceService := service.NewBalanceService(balanceUsecase)
	grpcServer := server.NewGRPCServer(confServer, observability, logger, balanceService)
	httpServer := server.NewHTTPServer(confServer, observability, logger, balanceService)
	registrar := server.NewRegistrar(consul)
	app := newApp(logger, grpcServer, httpServer, registrar)
	return app, func() {
		cleanup()
	}, nil
}
