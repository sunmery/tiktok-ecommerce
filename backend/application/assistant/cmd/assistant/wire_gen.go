// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"backend/application/assistant/internal/biz"
	"backend/application/assistant/internal/conf"
	"backend/application/assistant/internal/data"
	"backend/application/assistant/internal/server"
	"backend/application/assistant/internal/service"
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, confData *conf.Data, consul *conf.Consul, ai *conf.Ai, observability *conf.Observability, logger log.Logger) (*kratos.App, func(), error) {
	pool := data.NewDB(confData)
	client := data.NewCache(confData)
	dataData, cleanup, err := data.NewData(pool, client, logger)
	if err != nil {
		return nil, nil, err
	}
	orderRepo := data.NewAssistantRepo(dataData, logger)
	assistantUsecase := biz.NewAssistantUsecase(orderRepo)
	assistantService := service.NewAssistantService(assistantUsecase)
	grpcServer := server.NewGRPCServer(assistantService, confServer, observability, logger)
	httpServer := server.NewHTTPServer(confServer, assistantService, observability, logger)
	registrar := server.NewRegistrar(consul)
	app := newApp(logger, grpcServer, httpServer, registrar)
	return app, func() {
		cleanup()
	}, nil
}
